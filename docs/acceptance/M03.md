# M03 acceptance record

Date started: 2026-07-31

Gate status: **OPEN — do not start M04**

## Implemented

- Provider-neutral execution adapter with health, start, ordered events,
  cancellation, and structured final result.
- Raw ConnectRPC JSON adapter for AC `agentcompose.v2.RunService`, isolated
  from AC source packages.
- AC build metadata, required Docker driver, optional exact build-version pin,
  bearer authentication, bounded responses, and stable failure codes.
- Immutable repository/commit execution context, output schema, maximum
  two-hour request limit, and sandbox removal policy.
- Connect stream framing, byte-offset reconnect, terminal enforcement,
  malformed/dropped stream rejection, and deadline-driven `StopRun`.
- Literal redaction for the AC token and configured sensitive patterns before
  SSE or result delivery.
- M3 readiness dependency without coupling process liveness to AC.
- Admin/Maintainer-only internal start, SSE, result, and cancel endpoints with
  session and CSRF enforcement.
- Deliberately unlinked `/runs/diagnostic` UI. M4 task management remains
  unavailable.
- Non-sensitive AC project template at
  `deploy/agent-compose.repomender.yml`.

## Automated verification

The following checks pass:

```text
npm run verify
go test ./...
go vet ./...
```

Contract and HTTP tests cover:

- bearer authentication and the legacy `/api/version` envelope;
- offline, unauthorized, incompatible version, and missing runtime driver;
- immutable input rejection and exact `StartRun` request shape;
- Connect stream messages, terminal event, reconnect offset, malformed frame,
  dropped stream, and secret redaction;
- explicit cancel, deadline-triggered cancel, successful structured result,
  and failed-run classification before schema validation;
- unauthenticated, developer-role, and missing-CSRF rejection;
- ordered SSE output and provider-error message suppression.

The clean-stack and remote-provider deployment gates remain after the
real-provider work.

Post-fix verification completed:

```text
npm run verify                         PASS
go test -race ./...                    PASS
go vet ./...                           PASS
docker compose config --quiet          PASS
git diff --check                       PASS
```

Isolated clean-stack gate completed with a fresh PostgreSQL volume and M3
enabled against the local AC daemon:

```text
docker compose -p repomender-m3-gate up -d --build   PASS
GET /health/live                                     HTTP 200
GET /health/ready                                    HTTP 200
GET /                                              HTTP 200 (SSR shell)
GET /runs/diagnostic                               HTTP 200 (M3 UI)
docker compose -p repomender-m3-gate down --volumes  PASS
```

## Real AC evidence

Local daemon:

```text
URL: http://127.0.0.1:7410
version: 0
os/arch: linux/arm64
compiled driver: docker
project: repomender-diagnostics
project ID: ebfce4b61c4e5804db8e8a8cd2ecbbb46291ad4ab78448a9afeef5c123db814c
Agent: codex
```

The project template passed AC dry-run validation and was applied using the
already installed `ghcr.io/chaitin/agent-compose-guest:latest` image.

Real run `46973800f0d9004eced8df53bbf8d9a5226a28f6ea93bd4d3f62178256e4a815`
proved that RepoMender can start the Codex Agent, receive ordered log offsets,
and observe one terminal event. The run correctly ended as failed:

```text
AC Codex transport fell back from WebSockets to HTTPS.
The sandbox connection to api.openai.com ended during TLS/request transport.
```

Host direct access also fails, while the macOS system proxy on
`127.0.0.1:7897` returns the expected unauthenticated HTTP 401 from OpenAI.
After explicit user authorization, a temporary `0.0.0.0:17897` bridge exposed
that proxy to the AC Docker network. A container-side request then returned
OpenAI HTTP 401, proving outbound TLS transport through the proxy.

With the proxy configured temporarily on the AC diagnostic Agent, real run
`0c1b3adacff846213e95c9d8600625dcc47f40c7125983a6fc0544133e4568fd`
reached the configured model gateway and failed with
`Missing bearer or basic authentication` (historical evidence from before the
credential was configured).

After the AC model credential was configured and the user explicitly
authorized sending commit `00d62df5c0a6ef418a92d0004cf2c314a7590404` to the
configured model service, RepoMender started another real run:
`9cd529f55992a2e59208d16ea91515aaac87a19e6af0a29e89f7affb26da7638`.
The adapter received ordered events (`log`, `log`, `completed`) and correctly
classified the terminal failure as `ac_sandbox_failed`. AC reported HTTP 502
from its runtime model route and logged that model metadata for
`deepseek-v4-flash` was unavailable. The structured result was therefore not
produced. This remains an AC model-gateway/configuration blocker, not an
adapter contract failure.

The AC `.env` was then updated to `LLM_API_PROTOCOL=chat_completions` and the
daemon was restarted. A repeat run
`faaf61773d0c01b28ce3b172ff2049d4a88faff74a401cac67c6fc28eb8475d6` still
returned the same 502 and the same `/llm/openai/v1/responses` route. Read-only
inspection of AC's persisted configuration showed the `env_default` provider
remained pinned to `default_wire_api=responses`; AC's bootstrap intentionally
does not overwrite an already configured provider when the `.env` value
changes. The compatibility setting therefore has not reached the runtime
provider mapping yet.

The persisted provider and model mapping were then explicitly repaired to
`chat_completions`. AC daemon egress was temporarily routed through the
authorized local HTTPS bridge, and the diagnostic Agent received a matching
`NO_PROXY` entry for its internal `agent-compose` endpoint. This removed the
network and routing failures: the daemon reached the DeepSeek endpoint and the
latest run `e7b8ab6e60760c21ff17e2055013267adfa2f928d51c9e7b41a7e0aa1cc1c49c`
returned the upstream error
`response_format type is unavailable now`. DeepSeek accepted a direct
`chat_completions` request with `json_object`, but rejected the Codex/AC
bridge's `json_schema` response format. The RepoMender adapter consequently
classified the terminal state as `ac_agent_failed`.

Real cancellation run
`b97d4d9eecbdfe81a3a0618a0be6078a074a663019bba2f28c07c9765189a4b1`
passed: `StopRun` was accepted, AC persisted `RUN_STATUS_CANCELED`, SSE emitted
one terminal `canceled` event, and `Result` returned `ac_cancelled`.

After testing, the proxy variables were removed from the AC project, the
project was restored to the committed template as revision 14, the temporary
bridge process was terminated, the AC daemon proxy override was removed, and
port 17897 was confirmed not listening. The user subsequently requested the
official DeepSeek Responses endpoint; AC is now configured with
`LLM_API_ENDPOINT=https://api.deepseek.com/responses`,
`LLM_API_PROTOCOL=responses`, and persisted provider/model mappings of
`responses`. A credential-free probe reached that endpoint with HTTP 401,
confirming the URL is reachable. Credentials remain in AC only.

The diagnostic schema was tightened for DeepSeek Responses validation
(`schemaVersion` now declares `type: string`; additional properties are
rejected), and the adapter now prefers AC's agent `output` over runtime
metadata and extracts a schema envelope after provider preambles. The next
real run succeeded:
`704de22a8d3977987ab3e22ddd46ed9813990d2bf67731052960613c33d15cb7`.
It completed 15 ordered log/terminal events, AC persisted
`RUN_STATUS_SUCCEEDED`, and RepoMender returned a schema-valid `v1` result.

## Remaining gate items

- Replace the temporary smoke egress with a durable approved AC daemon egress
  path for deployments where direct outbound HTTPS is unavailable.
- Complete the remaining security/redaction and remote CI gates.
- Fill this record with final commands and evidence, create PR, pass remote
  CI, merge, and tag `m3-ac-execution-adapter-mvp`.
