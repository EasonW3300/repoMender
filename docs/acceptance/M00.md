# M00 acceptance record

Date: 2026-07-30

## Automated checks

- Frontend ESLint: passed.
- Frontend TypeScript check: passed.
- Vinext production build and server-rendered tests: 3/3 passed.
- Go unit tests with race detector: passed.
- Go static analysis with `go vet`: passed.
- Docker Compose configuration validation: passed.
- PostgreSQL migration integration test: passed.

## Clean-stack smoke test

A new Docker volume was created and the complete stack built successfully:

- PostgreSQL became healthy.
- API became healthy and applied migration `000001_foundation`.
- Worker started after API readiness.
- Gateway exposed the application on port 8088.
- `GET /health/live` returned HTTP 200 with `status=ok`.
- `GET /health/ready` returned HTTP 200 with `status=ready`.
- The root route rendered the RepoMender dashboard.
- PostgreSQL contained `schema_migrations` and `outbox_events`.

The migration integration test applied migrations twice, rolled back the latest
version, and successfully applied it again.

## Gate decision

M00 implementation and local acceptance requirements are satisfied. The module
may be submitted to remote CI. M01 must not be marked complete until its own
identity, security, and real OIDC acceptance evidence is recorded.
