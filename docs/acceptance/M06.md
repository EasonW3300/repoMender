# M06 acceptance record

Branch target: `feature/m6-ci-diagnosis`

Current workspace: branch `feature/m6-ci-diagnosis`; the M6 implementation and
real-provider replay evidence are committed locally, and the working tree is
clean. Remote PR/merge/tag gates remain pending.

Gate status: **OPEN — implementation, local acceptance, and authorized real-provider replay pass; repository gates pending**

## Implemented MVP slice

- Added `REPOMENDER_FEATURE_M6_CI_DIAGNOSIS`, gated on M2 SCM, M3 AC execution,
  M4 task core, and M5 code review.
- Normalized failed GitHub `workflow_run` events and created idempotent
  `ci_diagnosis` tasks bound to the workflow run and immutable head SHA.
- Added bounded GitHub Actions log archive download with ZIP entry and total
  size limits.
- Added UTF-8/binary validation, generic and configured-secret redaction, and
  line-preserving log chunk persistence.
- Added strict diagnosis result validation for root-cause hypotheses, evidence,
  confidence, reproduction outcome, and recommended action.
- Added M6 AC processor, durable findings/evidence, GitHub Check publication,
  and task-kind routing while preserving the M4 queue and M5 processor.
- Extended the local SCM fixture with a recorded Actions log archive and Check
  publication endpoint for the M6 provider smoke, including worker-side App
  key mounting and API base URL configuration.
- Added a reusable `compose.m6-smoke.yaml` overlay for the M3→M6 dependency
  chain and empty-database acceptance.
- Replaced the diagnostics list/detail UI mock path with API-backed durable
  `ci_diagnosis` task views.

## Verification completed

```text
M6 diagnosis/config contract tests                         PASS
M6 provider/log failure fixtures                           PASS
GitHub workflow webhook HTTP integration (auth/dedupe/ignore) PASS
Go race tests across ./...                                 PASS
go vet ./...                                                PASS
npm run lint                                                PASS
npm run typecheck                                           PASS
frontend build and rendered HTML tests (3/3)                PASS
docker compose config --quiet                              PASS
M6 empty-db Compose smoke (health/migrations/webhook/worker) PASS
M6 local AC/provider smoke (success/findings/publication)   PASS
Secret scan across M6 durable outputs                       PASS (0 matches)
Real private GitHub replay (logs/AC/Check)                   PASS (4 findings)
```

The complete local `make m6-verify` command passed. The isolated
`repomender-m6-smoke` project started from a new PostgreSQL volume; API and
gateway health returned HTTP 200, five migrations applied, a genuine signed
`workflow_run` created a `ci_diagnosis` task, and the duplicate delivery
returned `duplicate`. After the worker SCM secret mount was corrected, the
local no-secret Git fixture completed an AC diagnosis with four findings and
five persisted log/evidence records. The fixture Check publication returned
success, and the durable-output scan found zero occurrences of the fixture
secret.

## Real private-provider replay

The authorized replay used the private `EasonW3300/repomender-sandbox`
repository, installation `150053992`, real workflow run `30884243473`, and
immutable commit `83fe2934cf07523cb480ef5c40f078b39bf15cc9`. The real GitHub
Actions archive, repository checkout, job metadata, and Check API were used by
AC. The available source run had a successful native conclusion, so the
verified local webhook replay marked the delivery as `failure` to exercise the
M6 diagnosis branch without mutating the private repository.

The task succeeded with four hypotheses and real GitHub Check `92286307999`
completed with `action_required` on the exact commit. Check output and durable
M6 records had zero GitHub token-pattern matches. The replay also found and
closed two defects: the AC project GitHub token was stale, and AC stream NUL
bytes could reach PostgreSQL TEXT. The token was refreshed in the authorized AC
project environment; event text is now made valid UTF-8 and NUL-free before
persistence.

## Remaining gate items

- Run a native failed-workflow delivery. Local recorded fixtures already cover
  missing/expired logs, permissions, rate limits, binary output, oversized logs,
  cancellation, and non-reproducible diagnoses across the SCM, M6 processor,
  AC execution, and result-schema contracts.
- Complete the branch/PR/merge/tag workflow for `m6-ci-diagnosis-mvp`.
