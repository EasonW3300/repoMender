# M10 Acceptance — Enterprise delivery hardening

Status: **PASS (local M10 gate)**
Branch: `feature/m10-enterprise-hardening`
Feature flag: `REPOMENDER_FEATURE_M10_HARDENING`
Remote branch: pushed to `origin/feature/m10-enterprise-hardening`
Release tag: `m10-enterprise-delivery-hardening-mvp` pushed at `867c91e`
Pull request: pending GitHub write authorization (connector/CLI currently unavailable)

## Delivered implementation

- Added versioned M10 configuration and migration `000009_m10_hardening`.
- Added `/health/version`, Prometheus `/metrics`, bounded request metrics,
  bearer protection for metrics, W3C traceparent propagation, request IDs, and
  the fixed-window API/webhook limiter.
- Added transactional retention cleanup as `repomender retention` and a daily
  worker loop; pending/failed outbox work is preserved.
- Added atomic PostgreSQL backup and explicitly acknowledged restore scripts.
- Added non-root image build version injection, M10 Compose overlay, load test,
  and operator runbook.

## Verification record

```text
make m10-verify
go test -race ./...
go vet ./...
go test -p=1 -tags=integration ./internal/database ./internal/retention
```

All commands passed on 2026-08-06. The standard M10 target also passed frontend
lint/typecheck/build/render tests, Compose config validation, and shell syntax
checks. A disposable PostgreSQL 16 container passed migration repeatability,
rollback/re-upgrade, retention cleanup, and the existing M0–M9 integration
packages; it was stopped and removed afterward without touching the project
PostgreSQL volume. The bounded load test passed 512 requests with 32 workers.

The branch and release tag are published. PR creation remains a pending
external action because the GitHub connector returned 403 and the local CLI
credential refresh timed out; no PR has been claimed as created.
