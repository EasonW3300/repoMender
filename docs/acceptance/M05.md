# M05 acceptance record

Branch: `feature/m5-code-review`

Gate status: **PASS WITH EXTERNAL TEST CAVEAT — real private-repository review, publication, and supersession passed**

## MVP slice

- Added `REPOMENDER_FEATURE_M5_CODE_REVIEW`, gated on M2 SCM, M3 AC execution,
  and M4 task core.
- Extended the GitHub adapter to normalize `pull_request` actions with the
  repository, PR number, branches, immutable head SHA, author, and installation.
- Verified webhook deliveries are deduplicated before a review task is created.
- Created idempotent `code_review` tasks keyed by repository, PR number, and
  head SHA; newer commits supersede prior review tasks.
- Added strict v1 review-result validation (unknown fields, unsafe paths,
  invalid severities, line ranges, confidence, and finding count are rejected).
- Persisted findings and evidence through the task store and exposed them in
  task detail responses and the API-backed Code Reviews UI.
- Added the M5 worker processor: immutable commit execution through AC, ordered
  run events, structured result persistence, GitHub Check Run, and PR comment
  publication using short-lived installation tokens.
- Added explicit AC project and agent configuration (`REPOMENDER_AC_PROJECT_ID`
  and `REPOMENDER_AC_AGENT_NAME`) and a Responses-compatible strict schema whose
  required properties cover every nested Finding field.
- Supersession now matches the provider repository ID when an M2 repository
  snapshot is not present, which keeps webhook retries safe during initial sync.
- GitLab remains compiled but runtime-disabled.

## Automated verification

```text
npm run verify                                      PASS
GOCACHE=/tmp/repomender-gocache go test -race ./... PASS
GOCACHE=/tmp/repomender-gocache go vet ./...       PASS
git diff --check                                    PASS
docker compose config --quiet                       PASS
```

## Real smoke evidence

```text
Sandbox PR: https://github.com/EasonW3300/repomender-sandbox/pull/1
Latest actionable smoke commit: 5b8d74613f6a32e09031d846b82464e30462df42
Temporary HTTPS: https://avon-var-camcorder-investments.trycloudflare.com
Task/run result: succeeded / succeeded, attempt 2, 30 persisted events, 4 Findings
AC project revision: 15; codex receives GH_TOKEN/GITHUB_TOKEN as secret env
Token scope: selected repomender-sandbox repository, contents/metadata read, short-lived
Published Check: RepoMender Code Review, completed/action_required, exact head SHA
Published comment: GitHub issue comment 5175868487
```

The AC run and publication path are proven end to end against a private
repository. The review identified four actionable findings (CI supply-chain
pinning, ASCII-only normalization contract, malformed fixture timestamp, and
missing POSIX newline) and persisted their evidence, locations, confidence,
and remediation. The exact commit SHA is present on the Check Run and comment.

The short-lived installation token is injected only into the AC project secret
environment and is not stored in RepoMender or committed to this repository.

Supersession was verified: a newer head marked previous review tasks,
including the succeeded `85dac53d...` result, as `superseded`. Duplicate
delivery idempotency is covered by the automated webhook contract tests. The
GitHub App redelivery endpoint returned HTTP 401 in this environment, so a
provider-level redelivery attempt remains an external test caveat rather than
a product failure.
